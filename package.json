{
  "name": "lichess-kill-anim",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-userscript.mjs",
    "build:ext": "node scripts/build-extension.mjs",
    "package:ext": "node scripts/build-extension.mjs --zip",
    "build:gallery": "node scripts/build-gallery.mjs",
    "build:pages": "npm run build:gallery && cp -R dist/gallery/. docs/",
    "icons": "node scripts/generate-icons.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "chess.js": "^1.4.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "jsdom": "^26.1.0",
    "playwright": "^1.59.1"
  }
}
